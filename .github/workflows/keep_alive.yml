name: Keep alive

on:
  schedule:
    - cron: "0 8 * * *"  # tous les jours à 8h UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -s -o /dev/null -w "%{http_code}" https://pelinoredegalles.streamlit.app
